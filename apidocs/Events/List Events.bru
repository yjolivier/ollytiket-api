meta {
  name: List Events
  type: http
  seq: 1
}

get {
  url: {{OLLYTIKET_URL}}/events
  body: none
  auth: bearer
}

auth:bearer {
  token: {{LOGIN_TOKEN}}
}

docs {
  Lists the authenticated manager's events, newest first.
}
