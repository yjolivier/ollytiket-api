PORT=3000

DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=ollytiket
DB_PASSWORD=ollytiket
DB_NAME=ollytiket

# Set true only for local dev — auto-creates tables from entities.
# Use migrations instead once there's real data to preserve.
DB_SYNCHRONIZE=true

JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=30d
